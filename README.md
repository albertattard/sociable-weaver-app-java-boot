# Sociable Weaver

## Useful commands

- Run all tests

  ```shell
  $ ./gradlew check
  ```

- Run the application locally, ideal for development

  ```shell
  $ ./gradlew bootRun
  ```

- Build the application for production

  ```shell
  $ ./gradlew build
  ```
